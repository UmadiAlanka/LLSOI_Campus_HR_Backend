# Application
spring.application.name=LlsoiHrms
server.port=2027

# Database Configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# Ensure port 3307 is your correct MySQL port (standard is 3306, but keep 3307 if you configured it that way)
spring.datasource.url=jdbc:mysql://localhost:3307/employeemanagement
spring.datasource.username=root
spring.datasource.password=2002

# JPA / Hibernate
# STEP 1: Change 'update' to 'create' for ONLY ONE RESTART to fix the foreign key error
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Use the updated dialect for MySQL 8+
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=llsoicampus27@gmail.com
spring.mail.password=tjcr nxxm iwof zbmq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.com.klef.fsad.sdp=DEBUG

# Max file size
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Error handling
spring.web.error.include-message=always
spring.web.error.include-binding-errors=always
spring.web.error.include-stacktrace=on_param
spring.web.error.include-exception=false

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:3000,http://localhost:3001,http://localhost:5173
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true